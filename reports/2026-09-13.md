<!-- telegram-brief:start -->
🚀 Research decision
Pick: jackwener/OpenCLI #2489 — reject unsupported -f/--format values instead of silently rendering tables
Why/impact: Invalid formats currently exit successfully with misleading table output, which is especially risky for scripts/agents expecting a requested serialization format
Learning: CLI fail-closed validation, renderer contracts, regression-test design, cross-fork contribution workflow
Plan: Keep today’s useful OpenCLI fix as the single contribution; monitor the Draft PR for real feedback/CI and avoid manufacturing a second PR
Alternatives: Latest Repo-of-the-day alibaba/open-code-review is blocked by missing writable fork; github/spec-kit and other recent candidates were likewise blocked-by-fork-tooling

🧩 Contribution result
Contribution: Implemented unsupported-format rejection with regression coverage; user opened Draft PR #2491 successfully
Repository/issue: https://github.com/jackwener/OpenCLI / https://github.com/jackwener/OpenCLI/issues/2489
Branch/commit: https://github.com/gkmraju/OpenCLI/tree/fix/validate-output-format / 0da4ac33f90fb19c800f6cdb5ce8691cd5641f96, 0e07fdfb6c7a0d1ae1407baa13e6db926d429b1a
Validation: Diff inspected; branch was 2 commits ahead and 0 behind upstream when prepared; GitHub workflows are action_required with no jobs started, so no CI pass is claimed
Draft PR/fallback: https://github.com/jackwener/OpenCLI/pull/2491 — open, Draft, mergeable
Action needed: Optional cleanup: change PR title to "fix(output): reject unsupported formats"; integration cannot edit it (403)
<!-- telegram-brief:end -->

# Daily Contribution Report — 2026-09-13

## Repo-of-the-day priority

The latest Repo-of-the-day publication is `alibaba/open-code-review`, posted on 2026-09-13. Execution-readiness failed because `gkmraju/open-code-review` does not exist as a writable fork and the connected GitHub integration still exposes no usable fork-creation path.

The immediately prior candidate `github/spec-kit` was also blocked by the same writable-fork gate. Earlier recent candidates already evaluated today included `volcengine/OpenViking`, `ultralytics/ultralytics`, and `tesseract-ocr/tesseract`; each lacked a writable user fork, so none was used for implementation.

## Unfinished external work check

OpenCLI Draft PR #2490 (`fix(web): prefer larger main content over article cards`) remains open and Draft with no maintainer feedback requiring action.

OpenCLI Draft PR #2491 is now open successfully from `gkmraju/OpenCLI:fix/validate-output-format` into `jackwener/OpenCLI:main`. It is mergeable and still Draft. No maintainer comments or review feedback are present.

Its GitHub Actions runs currently report `action_required` for CI, Security Audit, and Doc Check, with no jobs started. Therefore no CI success or failure is claimed; this appears to require repository-side workflow approval/action before jobs can run.

## Selected repository and issue

Repository: `jackwener/OpenCLI`

Issue: #2489 — `[Bug]: -f with an unknown format silently renders a table instead of failing`

Why selected:
- The repository is execution-ready because `gkmraju/OpenCLI` is a writable fork.
- The issue is a correctness defect affecting scripts and agents, not a cosmetic change.
- Invalid serialization requests currently produce plausible-but-wrong table output instead of failing closed.
- The scope is small, independently reviewable, and testable.

## Duplicate and maintainer checks

Before implementation, issue context, relevant source, tests, contribution guidance, and duplicate PR risk were checked. No competing implementation PR for #2489 was found at selection time.

Relevant source observations:
- `src/help.ts` documents accepted public formats as `table, plain, json, yaml, md, csv`.
- `src/output.ts` previously allowed unknown formats to fall into table rendering.
- `src/output.test.ts` is the appropriate regression-test location.

## Implementation

Branch: `fix/validate-output-format`

Commits:
- `0da4ac33f90fb19c800f6cdb5ce8691cd5641f96` — `fix(output): reject unsupported formats`
- `0e07fdfb6c7a0d1ae1407baa13e6db926d429b1a` — `test(output): cover invalid format rejection`

Changes:
1. Added fail-closed validation for unsupported output formats.
2. Preserved internal aliases `markdown` and `yml`.
3. Replaced the switch fallback with explicit table handling so unknown values cannot silently succeed.
4. Added regression coverage verifying an invalid format throws and emits no output.

## Validation

The diff was inspected and the branch was confirmed 2 commits ahead and 0 behind upstream at preparation time. No local `npm test`, typecheck, or lint run is claimed because the connected environment did not provide a runnable repository checkout.

GitHub Actions are not yet providing validation evidence: CI, Security Audit, and Doc Check are all in `action_required` state and expose no started jobs.

## Draft PR status

Draft PR: https://github.com/jackwener/OpenCLI/pull/2491

State: open, Draft, mergeable.

Current PR title is `fix(output): reject unsupported formats Fixes #2489`; the description already contains `Fixes #2489`. A cleaner title would be `fix(output): reject unsupported formats`. An automatic title update was attempted in this run but the GitHub integration returned `403 Resource not accessible by integration`, so no metadata change was made.

## Expected impact

Callers that request an unsupported format now receive an explicit argument error instead of successful table output, preventing downstream automation from parsing the wrong serialization shape.

## Risks and limitations

- Repository CI has not executed yet; no pass/fail claim is made.
- Full local test/typecheck/lint execution was unavailable in the connected environment.
- Cross-fork PR metadata writes remain restricted by the current GitHub integration.
- No second contribution was created today because #2491 already satisfies the day’s external contribution objective and there is no responsible reason to manufacture additional activity.
