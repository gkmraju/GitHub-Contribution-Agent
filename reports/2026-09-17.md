<!-- telegram-brief:start -->
🚀 Research decision
Pick: jackwener/OpenCLI #2352 — align browser-realm LinkedIn URL credential rejection with the Node helper
Why/impact: Credential-bearing URLs currently survive browser extraction while Node rejects them; maintainer-authored issue documents the security inconsistency and a contributor confirmed the narrow credential-only scope.
Learning: Browser/page-realm isolation, URL credential parsing, LinkedIn safety redirect normalization, execution-readiness triage.
Plan: Add username/password rejection to all three browser-realm decoders, preserve relative-URL behavior, add focused regression tests, validate, then open a Draft PR.
Alternatives: onyx-dot-app/onyx was latest Repo-of-the-day but blocked-by-fork-tooling; active OpenCLI #2515 already has PR #2516; existing Drafts had no actionable feedback.

🧩 Contribution result
Contribution: Selected and branched a maintainer-grounded security fix; implementation not published because the available GitHub write interface requires whole-file replacement and the relevant large source file could not be safely reconstructed in-run.
Repository/issue: https://github.com/jackwener/OpenCLI ; https://github.com/jackwener/OpenCLI/issues/2352
Branch/commit: https://github.com/gkmraju/OpenCLI/tree/fix/linkedin-browser-credential-urls ; Not created
Validation: Verified current main still has three browser-realm decoders without username/password rejection; verified no PR currently references #2352.
Draft PR/fallback: No external change; never own-repo fallback
Action needed: Manual implementation can proceed on branch fix/linkedin-browser-credential-urls; no legal/CLA action taken.
<!-- telegram-brief:end -->

# Daily external contribution report — 2026-09-17

## Repo-of-the-day priority
The latest published Repo-of-the-day entry is `onyx-dot-app/onyx` (posted 2026-09-16 19:03 UTC). Execution-readiness was checked before deep implementation. No `gkmraju/onyx` fork exists and the connected GitHub integration currently exposes no working fork-creation action, so the candidate was marked **blocked-by-fork-tooling** and research immediately continued.

## Existing external work
Open Draft PRs in `jackwener/OpenCLI` were inspected. #2517, #2503 and #2490 remain open Drafts with no actionable maintainer comments returned by the connector. They were not used as repetitive monitoring work.

## Additional candidate checks
OpenCLI issue #2515 is a current, reproducible browser-navigation reliability bug, but PR #2516 already implements and validates the requested recovery ladder, so it was rejected as duplicate work.

## Selected opportunity
Selected `jackwener/OpenCLI#2352`, authored by the repository owner: browser-realm copies of `decodeLinkedInSafetyUrl` accept credential-bearing HTTP(S) URLs while the Node-side helper rejects `username`/`password`. The issue explicitly identifies three browser-realm copies: two in `clis/linkedin/profile-experience.js` and one in `clis/linkedin/profile-projects.js`. A recent contributor comment supports a narrow credential-only fix while preserving the legitimate relative-URL behavior of the browser realm. Global PR search for `2352` returned no existing PR.

## Execution readiness
`gkmraju/OpenCLI` is an existing writable fork with admin/push permission. Upstream `main` was verified at `8271afc67e8504bda94c147f446ee29775d08274`. Created focused branch `fix/linkedin-browser-credential-urls` from that exact upstream SHA.

## Source inspection
Current upstream source was inspected directly. All three browser-realm decoders still parse URLs against `location.origin` and accept any `http:`/`https:` target without checking `parsed.username`/`parsed.password` (or the corresponding redirect `target` credentials). Existing tests confirm extraction scripts at least parse as JavaScript, but do not pin the credential rejection behavior.

## Intended implementation
The smallest correct change is to reject credential-bearing URLs immediately after each browser-realm `new URL(...)` parse, including the decoded `/safety/go/?url=` target, while leaving relative URL resolution untouched. Focused tests should cover direct credential URLs, safety redirect credential targets, ordinary relative LinkedIn URLs, and existing non-HTTP rejection.

## Result / limitation
No contribution commit was created in this run. The connected GitHub write API can create branches and replace complete UTF-8 files, but does not expose a safe patch/hunk-edit action. One affected source file is large enough that the connector response is truncated when fetched whole; reconstructing and replacing it from partial output would risk accidental code loss. Rather than manufacture or publish an unsafe partial fix, the branch was left clean and the work remains Draft-stage research only.

No tests were run and no CI pass is claimed. No CLA, DCO, contributor agreement, legal attestation, merge, or ready-for-review action was performed.

## Manual action
If continuing manually, check out `gkmraju/OpenCLI:fix/linkedin-browser-credential-urls`, add the credential checks to all three browser-realm decoders, add focused tests, run the relevant Vitest adapter tests, push, and open a Draft PR referencing #2352. The automation should continue searching for fresh execution-ready work on the next run rather than using any user-owned repository as a contribution fallback.