<!-- telegram-brief:start -->
🚀 Research decision
Pick: jackwener/OpenCLI #2352 — reject credential-bearing URLs in LinkedIn browser-realm decoders
Why/impact: Security-relevant parity gap lets user:pass@ URLs survive browser extraction while Node rejects them
Learning: Browser/Node realm URL-normalization contracts, credential parsing, safe relative-URL preservation
Plan: Add username/password rejection to all three browser copies and focused regression coverage without changing relative URLs
Alternatives: roboflow/supervision latest Repo-of-the-day blocked-by-fork-tooling; OpenCLI #2519 rejected because current main already replaced the reported DOM path; existing Drafts had no actionable feedback

🧩 Contribution result
Contribution: Research and implementation plan completed; code not published because safe whole-file mutation of large browser-script sources is unavailable in the connected write path
Repository/issue: https://github.com/jackwener/OpenCLI/issues/2352
Branch/commit: https://github.com/gkmraju/OpenCLI/tree/fix/linkedin-browser-credential-urls ; no commit created
Validation: Verified issue owner’s measured divergence, contributor scope guidance, and current-main decoder copies; no executable tests run
Draft PR/fallback: No external change; never own-repo fallback
Action needed: Manual implementation/push is required unless patch-capable GitHub writes become available
<!-- telegram-brief:end -->

# Daily contribution report — 2026-09-18

## Repo-of-the-day
The latest published candidate is `roboflow/supervision` (posted 2026-09-17). The connected account has no accessible `gkmraju/supervision` fork, and the available GitHub integration exposes no fork-creation action. It therefore failed the execution-readiness gate as blocked-by-fork-tooling. Current open issues were still inspected enough to confirm there are worthwhile bugs, including #2594 (InferenceSlicer metadata merge conflict) and #2578 (LineZone treating tracker_id=-1 as a shared track), but implementation was not pursued because it could not be published.

## Existing external work
OpenCLI Draft PRs #2517, #2503, and #2490 remain open Drafts and showed no actionable maintainer feedback in the current metadata, so they were not used as repetitive monitoring work.

## Additional candidate checks
OpenCLI #2519 was evaluated because `gkmraju/OpenCLI` is writable and the issue reports a concrete Ctrip flight DOM regression. Current `main`, however, has already moved `clis/ctrip/flight.js` to structured `batchSearch` response interception and uses `.flight-item` only as a readiness signal, so reproducing the issue’s older selector-based patch would be stale/duplicative. It was rejected.

## Selected opportunity
OpenCLI #2352 remains a bounded, maintainer-authored security issue. The Node-side LinkedIn URL normalizer rejects URLs containing parsed usernames/passwords, while three page-realm `decodeLinkedInSafetyUrl` copies accept credential-bearing HTTP(S) URLs. A contributor comment explicitly recommends the narrow fix: reject `username`/`password` in the browser copies while preserving browser-relative URL resolution. Current code search confirms the browser copies remain in `profile-experience.js` (two copies) and `profile-projects.js` (one copy).

Expected impact: prevent credential-bearing absolute URLs and credential-bearing `/safety/go/?url=...` targets from leaking into LinkedIn extraction output, while retaining legitimate relative LinkedIn URL resolution in the page realm.

## Execution readiness and implementation status
A writable fork exists at `gkmraju/OpenCLI`, and branch `fix/linkedin-browser-credential-urls` exists. However, the connected mutation surface replaces complete UTF-8 files rather than applying a narrow patch. One affected browser-script source is large; reconstructing it from truncated tool output risks destructive unrelated edits. No unsafe replacement, fabricated commit, or low-confidence Draft PR was created.

## Intended minimal change
After each browser-realm `new URL(...)` parse, reject when `url.username || url.password`; apply the same check to decoded safety redirect targets. Add focused regression coverage for direct credentialed HTTP(S), credentialed safety redirects, `javascript:` rejection, and preservation of `/in/someone` origin resolution.

## Validation
Performed: Repo-of-the-day history verification; fork-readiness check; current OpenCLI issue/duplicate/context review; current-main code search for all decoder copies; inspection of current Ctrip implementation to reject stale #2519 work. Not performed: local Vitest/linters or CI, because no source commit was safely created.

## Risks and limitations
No external code contribution was published today. This is preferable to corrupting a large source file or manufacturing low-value work. The blocker is the lack of fork creation for the newest Repo-of-the-day and lack of a safe patch-style write operation for the selected large-file OpenCLI change.
