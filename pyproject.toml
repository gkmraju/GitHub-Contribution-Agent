[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "github-contribution-agent"
version = "0.1.0"
description = "Safety-first workflow for daily open-source contributions"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
github-contribution-agent = "github_contribution_agent.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
